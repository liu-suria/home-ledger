import { createSession, getSecrets, json, passwordMatches, readJson, sessionCookie } from "../../_lib.js";
export async function onRequestPost(context) { try { const { password } = await readJson(context.request); const { adminPassword, sessionSecret } = getSecrets(context); if (typeof password !== "string" || !(await passwordMatches(password, adminPassword))) return json({ error: "密码不正确" }, 401); return json({ ok: true }, 200, { "Set-Cookie": sessionCookie(await createSession(sessionSecret)) }); } catch (error) { return json({ error: error.message?.includes("configured") ? "服务端尚未配置登录密钥" : "登录失败，请稍后重试" }, error.message?.includes("configured") ? 503 : 400); } }
