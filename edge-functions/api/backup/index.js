import { json, readJson, requireAccess } from "../../_lib.js";
import { listBackups, readBackup, restoreBackup } from "../../_storage.js";
export async function onRequestGet(context){const a=await requireAccess(context);if(a.response)return a.response;try{const u=new URL(context.request.url),slot=u.searchParams.get("slot");if(slot===null)return json(await listBackups());const backup=await readBackup(Number(slot));return backup?json(backup):json({error:"备份不存在"},404)}catch(e){return json({error:e.message||"读取备份失败"},500)}}
export async function onRequestPost(context){const a=await requireAccess(context);if(a.response)return a.response;try{let slot=null;try{slot=(await readJson(context.request)).slot??null}catch{}return json(await restoreBackup(slot))}catch(e){return json({error:e.message||"恢复备份失败"},404)}}
